public class SnakeGame {
    public static void main(String[] arge){
        /* no need to do GameFrame frame = new GameFrame();
        since in this application we don't really need to use this GameFrame instance else where
        the new GameFrame(); will still create an instance of the gameframe
        */
        new GameFrame();
    }
}
